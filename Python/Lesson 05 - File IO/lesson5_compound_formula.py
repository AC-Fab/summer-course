# # def calculate_compound_interest():
# #     # Gather user input and convert string data to floating numbers
# #     p = float(input("Enter starting principal ($): "))
# #     annual_rate = float(input("Enter annual interest rate (%): "))
# #     n = int(
# #         input("Compounding periods per year (e.g., 12 for monthly, 1 for yearly): ")
# #     )
# #     t = float(input("Enter total number of years: "))

# #     # Convert percentage rate to a decimal format
# #     r = annual_rate / 100

# #     # Compound interest equation
# #     amount = p * (1 + r / n) ** (n * t)
# #     total_interest = amount - p

# #     print("\n--- Financial Breakdown ---")
# #     print(f"Initial Investment: ${p:,.2f}")
# #     print(f"End Balance:        ${amount:,.2f}")
# #     print(f"Total Profit:       ${total_interest:,.2f}")


# def money(principal: float, interest: float, t: int, n: int =1)
# # Execute the interactive console application
# # calculate_compound_interest()
# money(1000, 0.0061,)
